<div class="jumbotron">
  <h2>Come on in, the</h2>
  <h1>Water is Fine</h1>
  <p>Your portal to evolution. Be whatever you always dreamed.</p><p>Today.</p>
  <%= link_to "The Portal", dashboard_path, class: 'btn btn-lg btn-primary', role: 'button' %>
</div>
<% if @page != nil %>
<div class="container">
  <% if @links.length > 0 %>
  <div class="col-lg-7">
  <% else %>
  <div class="col-lg-12">
  <% end %>
    <p><%= @page.content %></p>
  </div>  
  <% if @links.length > 0 %>
  <div class="col-lg-5">
    <div class="well sidebar-nav">
      <ul class="nav nav-list">
        <li class="nav-header">Links</li>
        <% for link in @links %>
          <li><%= link_to link.name, link.url %></li>
        <% end %>
      </ul>
    </div><!--/.well -->
  </div><!--/span-->
  <% end %>
</div>
<% end %>
